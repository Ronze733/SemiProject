function changePlaceholder(element, message) {
	element.placeholder = message;
};

function restorePlaceholder(element, message) {
	element.placeholder = message;
};

function openPopup() {
    window.open("jsp/bj/login/pwRecovery.jsp", "Password Recovery", "width=700,height=600");
}

